# Задача

Добавить unit-test для `radix sort`.

# Сообщения пользователя

- `теперь давай начнем новый prompt и сбрось context, теперь мы добавляем radix sort unit test`
- `создай`

# Короткий лог

- Создан новый prompt file для отдельной задачи про `radix sort unit test`.
- Зафиксировано правило сборки: если нет явной необходимости в `Debug`, использовать `RelWithDebInfo`, потому что он сохраняет оптимизации и при этом оставляет отладочные символы.
